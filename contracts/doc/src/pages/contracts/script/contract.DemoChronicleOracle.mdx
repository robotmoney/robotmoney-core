---
title: "DemoChronicleOracle"
description: "Minimal Chronicle oracle stub for the RWA vault devnet demo. Always returns a fresh price (current block.timestamp) so the staleness check in RwaVault._checkOracleFreshness() never reverts during demo deploys. The price is set to 5000 USD/deSPXA (5000e18) as a plausible S&P 500 NAV reference. Demo-only; never deployed on mainnet."
---

# DemoChronicleOracle

[Git Source](https://github.com/robotmoney/robotmoney-core/blob/294ae939cc4db43ac7ec7bac05d27e475246f588/contracts/script/DeployDemoExtraVaults.s.sol)

**Inherits:** [IChronicleOracle](/contracts/interfaces/interface.IChronicleOracle)

Minimal Chronicle oracle stub for the RWA vault devnet demo.
Always returns a fresh price (current block.timestamp) so the
staleness check in RwaVault._checkOracleFreshness() never reverts
during demo deploys. The price is set to 5000 USD/deSPXA (5000e18)
as a plausible S&P 500 NAV reference. Demo-only; never deployed on mainnet.

## Constants

### DEMO_PRICE

Latest price in 18-decimal WAD format (1e18 = 1 USD).
Initialised to 5000 USD/deSPXA — a plausible S&P 500 NAV.

```solidity
uint256 public constant DEMO_PRICE = 5_000e18;
```

## Functions

<a id="latestanswer"></a>

### latestAnswer

Latest price returned by this stub oracle.

```solidity
function latestAnswer() external pure returns (uint256);
```

**Returns**

| Name | Type | Description |
| ---- | ---- | ----------- |
| &lt;none&gt; | `uint256` |  |

<a id="latesttimestamp"></a>

### latestTimestamp

Returns `block.timestamp` so the staleness check always passes.
Demo-only; a real Chronicle feed updates asynchronously.

```solidity
function latestTimestamp() external view returns (uint256);
```

**Returns**

| Name | Type | Description |
| ---- | ---- | ----------- |
| &lt;none&gt; | `uint256` |  |

