---
title: "ProtocolVaultBatchDeployer"
description: "Batch deployer #1 — the canonical `ProtocolAssetVault` (PRD §11.2) deployed inside a single broadcaster CREATE. Constructed with admin = adminAddr (the script broadcaster) so subsequent `addAsset` + registry calls remain on the broadcast key. Demo-only."
---

# ProtocolVaultBatchDeployer

[Git Source](https://github.com/robotmoney/robotmoney-core/blob/294ae939cc4db43ac7ec7bac05d27e475246f588/contracts/script/DeployDemoExtraVaults.s.sol)

Batch deployer #1 — the canonical `ProtocolAssetVault` (PRD §11.2)
deployed inside a single broadcaster CREATE. Constructed with
admin = adminAddr (the script broadcaster) so subsequent
`addAsset` + registry calls remain on the broadcast key. Demo-only.

## Constants

### protocolVault

```solidity
ProtocolAssetVault public immutable protocolVault;
```

## Functions

<a id="constructor-address-address-address-address-uint256-uint256"></a>

### constructor

```solidity
constructor(
        address usdc,
        address adminAddr,
        address emergencyResponder,
        address swapRouter,
        uint256 tvlCap,
        uint256 perDepositCap
    );
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| usdc | `address` |  |
| adminAddr | `address` |  |
| emergencyResponder | `address` |  |
| swapRouter | `address` |  |
| tvlCap | `uint256` |  |
| perDepositCap | `uint256` |  |

