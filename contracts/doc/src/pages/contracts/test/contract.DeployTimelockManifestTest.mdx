---
title: "DeployTimelockManifestTest"
description: "The manifest must answer, from one file: which chain, which addresses, which bytecode, holding which roles."
---

# DeployTimelockManifestTest

[Git Source](https://github.com/robotmoney/robotmoney-core/blob/db74d747cb63d7157485338d758ffc470a9ae2cf/contracts/test/DeployTimelock.t.sol)

**Inherits:** Test

The manifest must answer, from one file: which chain, which
addresses, which bytecode, holding which roles.

## Constants

### ADMIN_ROLE

```solidity
bytes32 public constant ADMIN_ROLE = keccak256("ADMIN_ROLE");
```

## State Variables

### harness

```solidity
ManifestHarness internal harness;
```

### manifest

```solidity
string internal manifest;
```

### outPath

```solidity
string internal outPath;
```

## Functions

<a id="setup"></a>

### setUp

```solidity
function setUp() public;
```

<a id="test_manifestrecordsthechainid"></a>

### test_manifestRecordsTheChainId

```solidity
function test_manifestRecordsTheChainId() public view;
```

<a id="test_manifestrecordsaddresses"></a>

### test_manifestRecordsAddresses

```solidity
function test_manifestRecordsAddresses() public view;
```

<a id="test_manifestrecordscodehashes"></a>

### test_manifestRecordsCodeHashes

Code hashes are what make the manifest an identity record rather
than an address list: two deployments at the same address on two
chains are distinguishable only by bytecode.

```solidity
function test_manifestRecordsCodeHashes() public view;
```

<a id="test_manifestrecordsther7roleconditions"></a>

### test_manifestRecordsTheR7RoleConditions

The two R7 conditions, recorded as booleans an auditor can grep.

```solidity
function test_manifestRecordsTheR7RoleConditions() public view;
```

<a id="test_manifesttests_passanexplicitpath_neversetdeploymentout"></a>

### test_manifestTests_passAnExplicitPath_neverSetDeploymentOut

Every manifest test in this file passes its output path to
`exposedWriteJsonTo`. None sets `DEPLOYMENT_OUT`: env vars are
process-wide and forge runs test contracts in parallel, so a
shared variable lets one test write to another test's path.

```solidity
function test_manifestTests_passAnExplicitPath_neverSetDeploymentOut() public view;
```

<a id="test_manifestrecordsthequorumflooritwasdeployedunder"></a>

### test_manifestRecordsTheQuorumFloorItWasDeployedUnder

```solidity
function test_manifestRecordsTheQuorumFloorItWasDeployedUnder() public view;
```

<a id="test_manifestrecordszerolistedagents_whennonelisted"></a>

### test_manifestRecordsZeroListedAgents_whenNoneListed

A run with no listed agent records a listed count of zero, so
`deployer_owns_a_listed_gateway_agent = false` is not read as a
check over every agent (issue #1476).

```solidity
function test_manifestRecordsZeroListedAgents_whenNoneListed() public view;
```

