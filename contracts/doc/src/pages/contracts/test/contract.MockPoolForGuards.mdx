---
title: "MockPoolForGuards"
---

# MockPoolForGuards

[Git Source](https://github.com/robotmoney/robotmoney-core/blob/294ae939cc4db43ac7ec7bac05d27e475246f588/contracts/test/ConfusedDeputyGuards.t.sol)

<i>

Uniswap V3 pool stub. token0/token1 are set at construction.
observe() returns linear tick cumulative (tick rate 0 → 1:1 price).
cardinality is settable so addAsset cardinality checks can be tested.

</i>

## Constants

### token0

```solidity
address public immutable token0;
```

### token1

```solidity
address public immutable token1;
```

## State Variables

### cardinality

```solidity
uint16 public cardinality;
```

### poolLiquidity

```solidity
uint128 public poolLiquidity;
```

## Functions

<a id="constructor-address-address"></a>

### constructor

```solidity
constructor(address token0_, address token1_);
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| token0_ | `address` |  |
| token1_ | `address` |  |

<a id="fee"></a>

### fee

<i>

ORA-3 / F-09: `addAsset` asserts `fee()` equals `swapFee_`. Every
addAsset call in this suite uses swapFee_ = 500.

</i>

```solidity
function fee() external pure returns (uint24);
```

**Returns**

| Name | Type | Description |
| ---- | ---- | ----------- |
| &lt;none&gt; | `uint24` |  |

<a id="setcardinality-uint16"></a>

### setCardinality

```solidity
function setCardinality(uint16 c) external;
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| c | `uint16` |  |

<a id="setliquidity-uint128"></a>

### setLiquidity

```solidity
function setLiquidity(uint128 l) external;
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| l | `uint128` |  |

<a id="liquidity"></a>

### liquidity

```solidity
function liquidity() external view returns (uint128);
```

**Returns**

| Name | Type | Description |
| ---- | ---- | ----------- |
| &lt;none&gt; | `uint128` |  |

<a id="slot0"></a>

### slot0

```solidity
function slot0() external view returns (uint160, int24, uint16, uint16, uint16, uint8, bool);
```

**Returns**

| Name | Type | Description |
| ---- | ---- | ----------- |
| &lt;none&gt; | `uint160` |  |
| &lt;none&gt; | `int24` |  |
| &lt;none&gt; | `uint16` |  |
| &lt;none&gt; | `uint16` |  |
| &lt;none&gt; | `uint16` |  |
| &lt;none&gt; | `uint8` |  |
| &lt;none&gt; | `bool` |  |

<a id="observe-uint32"></a>

### observe

```solidity
function observe(uint32[] calldata secondsAgos)
        external
        view
        returns (int56[] memory tickCumulatives, uint160[] memory secondsPerLiq);
```

**Parameters**

| Name | Type | Description |
| ---- | ---- | ----------- |
| secondsAgos | `uint32[]` |  |

**Returns**

| Name | Type | Description |
| ---- | ---- | ----------- |
| tickCumulatives | `int56[]` |  |
| secondsPerLiq | `uint160[]` |  |

