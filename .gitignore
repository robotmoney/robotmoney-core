node_modules/
dist/
coverage/
.turbo/
*.log
.DS_Store
.env
.env.*
!.env.example
.ows/

# Foundry
out/
cache/
broadcast/
foundry.lock
lcov.info

# Rust / Cargo (Cargo.lock is checked in for binary crates)
target/

# Per-machine Makefile overrides (tunnel hostnames, local paths, etc.)
Makefile.local

# Internal planning docs — not for public history
plan.md

# Confidential source papers (verbatim conversions + originals).
# Kept local-only; analysis lives in docs/papers/open-questions.md.
docs/papers/Robot-Money-Whitepaper-v01.md
docs/papers/robot_money_plan_v4.md
docs/papers/robot_money_prd.md
docs/papers/*.pdf
docs/papers/*.docx

# Local agent prompts (skills/commands/workflows) — staging dir + Claude Code state
.agents/
.claude/

# Phase 7 demo run artifacts (per-run dirs under artifacts/demo/<timestamp>/).
# Captured by testing/demo/demo.sh; uploaded by .github/workflows/demo.yml.
artifacts/
