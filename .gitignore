node_modules/
dist/
coverage/
.turbo/
*.log
.DS_Store
.env
.env.*
!.env.example
.ows/

# Foundry
out/
cache/
broadcast/
foundry.lock
lcov.info

# Rust / Cargo (Cargo.lock is checked in for binary crates)
target/

# Internal planning docs — not for public history
plan.md

# Local agent prompts (skills/commands/workflows) — staging dir + Claude Code state
.agents/
.claude/

# Phase 7 demo run artifacts (per-run dirs under artifacts/demo/<timestamp>/).
# Captured by testing/demo/demo.sh; uploaded by .github/workflows/demo.yml.
artifacts/
